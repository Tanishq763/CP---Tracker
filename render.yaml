services:
  - type: web
    name: cp-tracker-server
    runtime: node
    rootDir: server
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: GROQ_API_KEY
        sync: false
      - key: ANALYTICS_PASSWORD
        sync: false
      - key: PORT
        value: 5000
